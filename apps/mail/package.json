{
  "name": "mailmind-mail",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "scripts": {
    "dev": "node -e \"console.error('\\n  Mail UI is Electron-only (not a web app).\\n  From repo root: make desktop\\n  Build dist only: npm run build\\n'); process.exit(1)\"",
    "build": "tsc -b && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^19.2.5",
    "react-dom": "^19.2.5",
    "react-router-dom": "^7.15.0"
  },
  "devDependencies": {
    "@types/react": "^19.2.2",
    "@types/react-dom": "^19.2.2",
    "@vitejs/plugin-react": "^6.0.1",
    "typescript": "^6.0.3",
    "vite": "^8.0.10"
  }
}
